#include "main.h"
/**
 * _strlen - calculate length of a string
 * @str: string
 *
 * Return: i
 */
ssize_t _strlen(const char *str)
{
	ssize_t i = 0;

	while (str[l] != '\0')
	{
		i++;
	}
	return (i);
}
